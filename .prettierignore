**/node_modules
**/dist

# Generated / tool-owned files: reformatting them creates noise and, for the
# lockfile, a diff on every install.
pnpm-lock.yaml
.claude/

# Vendored upstream: keep byte-identical so it can be diffed against the
# gristlabs/grist-widget copy when it is refreshed.
shared/grist/grist-plugin-api.d.ts

# Generated by shared/infrastructure/insee/generate-departements-contours.ts:
# one département per line keeps 100 000 coordinates diffable, where Prettier
# would give each number a line of its own.
shared/infrastructure/insee/departements-contours.json
