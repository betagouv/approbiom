# This (empty) file marks "provenance" as a Python package.
# Without it, pytest puts scripts/import/provenance/ on sys.path instead of
# scripts/import/, and the tests' "from provenance.… import" fails.
